to build this task please run the run_script.sh in the directory by using ./run_script.sh
in the script the commands to cmake the project and generate the makefile in the build directory 
the script also requires from the user to enter either:
1. -f to write to a file and show the content of this file 
2. -c to write hello world 
this documentation was created by Abdelrhman Elknawy .
